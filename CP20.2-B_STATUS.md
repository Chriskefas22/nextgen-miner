# CP20.2-B Economic Integrity Patch

Status: PREPARED — NOT APPLIED

This patch is intentionally limited to confirmed CP20.2-A economic integrity findings.

Changes:
1. `faucet_20` target 20 -> 10 so it matches the live 50-Diamond/day cap with a 5-Diamond claim.
2. Disable `offer_5`, `ptc_100`, and `shortlink_10` while their providers are not live.
3. Harden `nextgen_upgrade_miner()` against already-merged miner rows.
4. Keep PTC/Offers/Shortlinks provider activation untouched.
5. Keep UI changes out of this economic patch.

Apply order:
1. Push migration file to GitHub.
2. Apply the migration once to production Supabase.
3. Run `verification/CP20.2_B_verify.sql`.
4. Verify Vercel redeploy/build.
5. Re-test `/miners`, `/merge`, `/quests`, `/faucet`.
6. Then make the separate CP20.2-A UI honesty/navigation patch for P0/P1 route findings.
