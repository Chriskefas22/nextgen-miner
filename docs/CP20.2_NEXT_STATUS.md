# CP20.2 Next Status

## Applied directly to Supabase production
- CP20.2-B quest alignment: faucet quest target 10; provider-dependent quests disabled.
- CP20.2-B merged-row upgrade guard.
- Corrective integrity: default inactive-membership factor/discount to 1.0/0 and keep capacity guard active.
- Corrective integrity: fix `nextgen_quests_snapshot()` period-key ambiguity.
- CP20.2-C: revoke anon/authenticated EXECUTE from internal CP18/CP19/test SECURITY DEFINER RPCs. Landing telemetry remains public by design.

## Verified
- Purchase with active user and no active membership is no longer bypassed by NULL membership factor; capacity guard blocks expansion at current CRITICAL state.
- Upgrade no longer produces NULL `final_price`; it reaches the same capacity guard and blocks expansion at current CRITICAL state.
- Merge of two identical standard miners succeeds using configured level-1 fee of 25 Diamond when no additional funded capacity is required in the test transaction.
- Faucet test returns 5 Diamond and respects the 50 Diamond/day cap.
- Quests snapshot returns only enabled quests; faucet quest is 10 claims. A transactional 10-claim fixture successfully completes quest and returns 100 Diamond reward. All test data was rolled back.
- Security advisors: authenticated SECURITY DEFINER warnings reduced from 47 to 40; anonymous reduced from 5 to 1. The remaining anonymous function is `nextgen_get_landing_telemetry()`, intentionally public.

## Not pushed to GitHub
GitHub write integration returned HTTP 403 while attempting to create the P0 deposit route. The attached package is push-ready; production DB changes are already applied.

## P0 source patch
1. Add `app/wallet/deposit/page.tsx` so the existing Topbar `/wallet/deposit` CTA resolves.
2. Replace hardcoded fake LIVE shortlinks with an explicit NOT LIVE state until a verified provider/campaign is active.
