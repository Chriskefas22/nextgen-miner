MANDATORY DELETE:
middleware.ts

The current repository still contains a root `middleware.ts`, while the project also has a root `proxy.ts`.
Vercel/Next.js therefore aborts the build with:
"Both middleware file './middleware.ts' and proxy file './proxy.ts' are detected. Please use './proxy.ts' only."

After deleting `middleware.ts`, the only routing entry point should be:
proxy.ts
which imports:
@/lib/supabase/proxy -> updateSession
