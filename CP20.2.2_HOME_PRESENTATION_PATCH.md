# CP20.2.2 — HOME PRESENTATION REFINEMENT

Applied after authenticated mobile screenshots:

- Restored the missing holographic N Core by defining the hero core classes used by HomeCommandCenter.
- Kept POWER / CONNECTS / PEOPLE as a deliberate hero annotation rather than an empty visual field.
- Removed the generic green asset tile background so each crypto logo is the visual focal point; logos are enlarged.
- Removed the redundant Network Snapshot panel from the user-facing Home. Its operational data remains available from the server snapshot and other protected surfaces.
- Rebalanced the lower Home grid from three columns to two after removing Network Snapshot.
- No Supabase RPC/economic SQL changes.
- Landing page remains untouched.
