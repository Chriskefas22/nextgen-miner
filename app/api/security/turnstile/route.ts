import {NextResponse} from 'next/server';import {verifyTurnstile} from '@/lib/security/turnstile';export async function POST(req:Request){const {token}=await req.json().catch(()=>({token:''}));if(!token)return NextResponse.json({success:false,error:'missing_token'},{status:400});const result=await verifyTurnstile(token);return NextResponse.json(result,{status:result.success?200:403})}
