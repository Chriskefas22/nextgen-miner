import Link from 'next/link';
import { Clock3, Headphones, Settings, ShieldCheck, Trophy, Users, Wallet } from 'lucide-react';
import { AppShell } from '@/components/layout/AppShell';
const links=[['Wallet','/wallet',Wallet,'Crypto balances and internal Diamond balance.'],['History','/wallet/history',Clock3,'Transaction and mining ledger history.'],['Referrals','/referrals',Users,'Referral code and activity.'],['Leaderboard','/leaderboard',Trophy,'Network ranking interface.'],['Support','/support',Headphones,'Contact the support system.'],['Settings','/settings',Settings,'Account preferences.']] as const;
export default function MorePage(){return <AppShell><div className="page-head"><div><div className="eyebrow">MORE</div><h1 className="page-title">System Hub</h1><div className="muted">Secondary account tools stay here so the core mining experience remains focused.</div></div></div><section className="glass section" style={{marginBottom:14}}><div className="section-head"><div><div className="eyebrow">CORE-FIRST MODE</div><h2 className="section-title">Financial/provider rails are intentionally deferred.</h2></div><ShieldCheck size={20}/></div><p className="muted">Deposit, withdrawal, Shortlinks, PTC, provider integrations and settlement automation are not part of the current core build pass.</p></section><div className="grid grid-3">{links.map(([label,href,Icon,description])=><Link href={href} key={label} className="glass section"><Icon size={21}/><h2 className="section-title">{label}</h2><p className="muted">{description}</p><span className="trend">Open →</span></Link>)}</div></AppShell>}
