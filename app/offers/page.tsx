import {AppShell} from '@/components/layout/AppShell';export default function Offers(){return <AppShell><div className="page-head"><div><div className="eyebrow">OFFERWALL</div><h1 className="page-title">Offers & Surveys</h1><div className="muted">Rewards post only after partner callback validation.</div></div></div><div className="grid grid-3">{['Timewall','CPX Research','BitLabs'].map(x=><section className="glass section" key={x}><span className="badge green">CONNECTED</span><h2 style={{fontFamily:'Orbitron',fontSize:14}}>{x}</h2><p className="muted">Provider catalog will populate through server-side configuration.</p><button className="btn btn-primary" style={{width:'100%'}}>Browse Offers</button></section>)}</div></AppShell>}