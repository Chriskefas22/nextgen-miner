'use client';import {useEffect,useState} from 'react';import {AppShell} from '@/components/layout/AppShell';export default function Faucet(){const[sec,setSec]=useState(0);useEffect(()=>{const id=setInterval(()=>setSec(s=>Math.max(0,s-1)),1000);return()=>clearInterval(id)},[]);const can=sec===0;return <AppShell><div className="page-head"><div><div className="eyebrow">FREE REWARDS</div><h1 className="page-title">Faucet</h1><div className="muted">Timed claim cycle with server-side cooldown.</div></div></div><div className="grid grid-2"><section className="glass section" style={{textAlign:'center'}}><div className="holo-core" style={{height:300}}><img className="holo-preview" src="/assets/nextgen-master-ui.png"/></div><div className="eyebrow" style={{marginTop:12}}>NEXT CLAIM</div><div className="stat"><b>{can?'READY':`${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}</b></div><button className="btn btn-primary" disabled={!can} onClick={()=>setSec(180)}>CLAIM NOW</button><p className="muted" style={{fontSize:11}}>Reward amount is calculated by the current faucet economy configuration.</p></section><section className="glass section"><div className="eyebrow">RULES</div><h2>Claim safely</h2><div className="list-row"><span className="muted">Cycle</span><b>3 hours</b></div><div className="list-row"><span className="muted">Reward display</span><b>💎 variable</b></div><div className="list-row"><span className="muted">Bot protection</span><span className="badge green">TURNSTILE</span></div><div className="list-row"><span className="muted">Cooldown</span><span className="badge green">SERVER</span></div></section></div></AppShell>}