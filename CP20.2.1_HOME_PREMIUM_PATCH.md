# CP20.2.1 — HOME PREMIUM VISUAL POLISH

Purpose: polish the authenticated Home dashboard based on the supplied mobile screenshots without changing the frozen public landing page or economic SQL.

Files:
- `components/home/HomeCommandCenter.tsx` — premium inline crypto logos, selected asset ordering, safer coverage label, cleaner CTA wording, explicit non-button earnings period, live/global labels.
- `components/layout/AppShell.tsx` — loads the Home-only polish stylesheet.
- `styles/home-premium-polish.css` — premium holographic N core, crypto-tile styling, guarded metric presentation, drawer X/logout button reset, mobile spacing.

Important:
- Home TSX in this patch is the locally syntax-checked premium revision.
- Supabase RPC names/contracts are unchanged.
- Landing page components/assets are untouched.
- Push the three files into `main`; Vercel will redeploy from Git.
