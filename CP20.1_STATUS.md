# CP20.1 — FINAL CORRECTED SOURCE PACKAGE

This package was corrected after audit because the first CP20.1 ZIP did **not**
contain the two most important modified source files:
- app/miners/page.tsx
- app/dashboard/page.tsx

Those files are included here as complete source files.

GitHub verification:
- `app/miners/page.tsx` EXISTS on `main`; the prior ZIP was simply missing it.
- `app/dashboard/page.tsx` EXISTS on `main`; the prior ZIP was simply missing it.
- Current GitHub miners source hardcoded merge fees; this package removes that and
  loads `nextgen_merge_fee_snapshot()`.
- Current GitHub dashboard displayed a hardcoded 30% reserve; this package derives
  reserve/mining percentages from the DB snapshot.

CP20.1 explicit files:
1. app/miners/page.tsx
2. app/dashboard/page.tsx
3. app/faucet/page.tsx
4. app/quests/page.tsx
5. app/ptc/page.tsx
6. app/surveys/page.tsx
7. app/merge/page.tsx
8. components/layout/Sidebar.tsx
9. supabase/migrations/20260915_cp20_1_final_sync.sql
10. tools/apply_cp20_1.py

The migration SQL reflects the already-applied live Supabase changes. Do not
re-apply blindly to production if your migration history already contains the
same function definitions.

Landing remains intentionally frozen.

Vercel deployment must happen only after these source files are committed/pushed.
