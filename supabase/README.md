# Supabase

NextGen Miner production database has been provisioned in Supabase project `mmqprhuvhghyuvudsyma`.

The frontend uses the Supabase publishable key only. Never add the service-role key to GitHub or client-side code. Cloudflare Turnstile secret belongs only in Vercel environment variables.
